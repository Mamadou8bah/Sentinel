# Case management

**Owns:** compliance review queue and decisions.

## What’s here now

- Auto-open from KYC / document / TX scoring
- `GET /api/cases`, `GET /api/cases/{id}`
- `PATCH /api/cases/{id}/decision` — note required; audited

WebSocket broadcast to staff UI is next.
