# Admin module

**Owns:** tenant configuration for ops.

## What’s here now

- `GET/PUT /api/admin/settings/risk-weights` (`ADMIN` only)
- Changes are audited

Analytics endpoints (fraud trend) come later.
