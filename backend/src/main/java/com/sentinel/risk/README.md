# Risk scoring engine

**FR:** FR-17–20  
Combine KYC + document fraud + transaction anomaly into 0–100 weighted score + human-readable explanation. Recalculate on new evidence. Admin-configurable weights/thresholds.
