# Dockerfile added when backend code exists.
# Expected: eclipse-temurin:21-jre, expose 8080, jar from target/
FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
# COPY target/sentinel-backend-*.jar app.jar
# ENTRYPOINT ["java","-jar","/app/app.jar"]
CMD ["echo", "sentinel-backend scaffold — build not ready"]
