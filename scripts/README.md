# Scripts (later)

Planned contents (do not implement yet):

| Script | Purpose |
|--------|---------|
| `train_signature_model.py` | Train Siamese CNN on CEDAR |
| `train_isolation_forest.py` | Fit PaySim Isolation Forest → `.joblib` |
| `generate_synthetic_docs.py` | Template-based fake IDs/cheques |
| `seed_demo_users.sql` | Admin / Compliance / Analyst seed accounts |
| `e2e_smoke.sh` | Demo-path health checks |

Keep training entrypoints here or under `services/*/scripts` — decide when implementing.
